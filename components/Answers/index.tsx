import { TAnswers } from "../../typings"
import Answer from "./Answer";

const Answers = ({ answers, lang }: { answers: TAnswers, lang: string }) => {
    // answers[lang].map((answer) => console.log(answer))
    return (
        <div></div>
    )


}

export default Answers